apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bgpalerter.fullname" . }}-test-connection"
  labels:
    {{- include "bgpalerter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bgpalerter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
