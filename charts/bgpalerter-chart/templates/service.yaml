apiVersion: v1
kind: Service
metadata:
  name: {{ include "bgpalerter.fullname" . }}
  labels:
    {{- include "bgpalerter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bgpalerter.selectorLabels" . | nindent 4 }}
